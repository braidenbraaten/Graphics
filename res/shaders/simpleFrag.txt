#version 150

//This is GLSL!

in vec4 vPosition;
in vec4 vColor;
out vec4 outColor;
void main() 
{
	//One's Complement (aka , color inversion)
	//1 - val = One's Complement
	


	outColor = vColor;
	outColor.g = abs(cos(vPosition.x*100));
	//outCOlor.r = (cos(vPosition.y*100) +1) /2;


	
}